{"default":{"identifier":"default","description":"Least-privilege surface for the LocalConvert window. The frontend may subscribe to job events and open a native directory picker. It is granted no filesystem, shell, process, HTTP or opener permissions: every file operation goes through a typed Rust command that validates its arguments.","local":true,"windows":["main"],"permissions":["core:event:default","dialog:allow-open"]}}